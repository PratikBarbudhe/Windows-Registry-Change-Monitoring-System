# Core runtime
# `pywin32` is Windows-only and should be skipped on Linux (e.g., Streamlit Cloud).
pywin32>=305; platform_system == "Windows"
flask>=3.0.0

# Optional formatting/console helpers
colorama>=0.4.6
tabulate>=0.9.0

# Test and quality tooling
pytest>=7.0
pytest-cov>=3.0
